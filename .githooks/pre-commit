#!/bin/sh
set -eu

if ! command -v yq >/dev/null 2>&1; then
  echo "pre-commit: yq is required but was not found in PATH" >&2
  exit 1
fi

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

./thousand-lines-of-glorious-bash.sh
git add feed.rss
